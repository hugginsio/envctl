# envctl

Connect your environment files to your own secret managers.

## Usage

While in v0, `envctl` must be installed using the Go toolchain:

```sh
go install github.com/hugginsio/envctl@main
```
